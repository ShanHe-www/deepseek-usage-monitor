# DeepSeek 用量全史（浏览器插件）

突破官网 **30 天限制**，一键抓取并永久保存全部 DeepSeek API 用量历史，图表可视化。
界面仿照官方 `platform.deepseek.com/usage` 的**浅色风格**（靛蓝配色、卡片、热力图、明细表）。

> 数据**仅保存在你的浏览器本地**（IndexedDB），不上传任何服务器。

## ✨ 功能

- 🎨 **官方风格界面**：靛蓝浅色主题，仿官方用量页布局
- 📆 **自定义日期范围**：全部 / 近12月 / 近30天 / 近7天 / 本月 / 任意起止日期
- 🔥 **按天热力图**：GitHub 风格，点击某天查看明细（官方同款）
- 📋 **每日各模型明细表**：输出 Token、缓存命中/未命中、命中率、花费 + Total 合计行
  - **按模型** / **按 Key × 模型** 两种粒度可切换
- 📈 **Token 消耗趋势**：按天 / 按月切换
- 🧱 **模型每日 Token（堆叠）**：X=日期，每模型一根堆叠柱，柱内分：输入·缓存命中 / 输入·缓存未命中 / 输出（三段配色参考官方），用量第一的模型为蓝色系，点击柱子选中该天
- 💰 **每日费用条形图**：每天一根柱 = 当日总花费（仿官方 Daily cost），点击柱子选中该天
- 🥧 **模型用量分布**：v4-pro / v4-flash 等各模型占比
- 🔑 **按 API Key 汇总**：每个 Key 的输入 / 输出 Token、费用、请求数
- 🕐 **全量历史**：从 2024 年起逐月拉取，突破官网 30 天限制
- ⚡ **打开弹窗自动同步当月**：静默更新，无需手动点
- ⬇ **导出 CSV**：把所选范围明细导出为本地文件
- 🔍 **诊断**：一键检查页面/登录凭证/接口状态，定位同步问题
- 🔒 **本地存储**：IndexedDB，数据永不离开本机

## 📂 项目结构

```
deepseek-usage-monitor/
├── manifest.json          # 插件配置（Manifest V3）
├── background.js          # 后台 Service Worker：IndexedDB + 消息路由
├── content.js             # 注入脚本：抓取 DeepSeek 后台数据
├── lib/
│   ├── csv.js             # CSV 解析与聚合（零依赖）
│   ├── chart.umd.min.js   # Chart.js（图表库）
│   └── fflate.umd.js      # ZIP 解压（用于解析导出包）
├── popup/
│   ├── popup.html         # Dashboard 界面
│   ├── popup.js           # 图表渲染与交互
│   └── popup.css          # 样式
├── icons/                 # 插件图标（16/48/128）
├── scripts/
│   ├── make-icons.js      # 重新生成图标（可选）
│   └── test-csv.js        # CSV 解析单测（node scripts/test-csv.js）
└── demo/                  # 浏览器预览 Demo（模拟数据）
```

## 🚀 安装（本地加载）

1. 打开 Chrome，地址栏输入 `chrome://extensions` 回车
2. 打开右上角 **开发者模式**
3. 点击 **加载已解压的扩展程序**
4. 选择本目录 `deepseek-usage-monitor` 文件夹
5. 浏览器工具栏固定插件图标 ✅

## 📖 使用

1. 在 Chrome 中打开并登录 **[platform.deepseek.com](https://platform.deepseek.com)**（用量页也行，登录状态是必须的）
2. 点击插件图标 → 点 **🔄 同步全部历史**
3. 插件会从 2024 年 1 月起逐月抓取所有月份的用量明细，存入本地
4. 抓取完成后自动刷新图表，之后随时打开插件图标即可查看全部历史

**提示**：
- 首次同步会逐月请求，约几十秒；之后再次同步只拉取「当月」和「之前为空的新月份」，非常快
- 建议每月打开一次插件点一下同步，历史就持续累积

### 🔍 同步不到数据？先用「诊断」按钮

点击插件弹窗里的 **🔍 诊断**，插件会现场检查并告诉你每一步的结果，例如：

```
页面: ✅ platform.deepseek.com
登录凭证: ✅ eyJhbGci…(128字)
localStorage 疑似 token 的 key: userToken
cookies: auth, session
导出接口: HTTP 200 / 2048 字节 / ZIP — OK
账户接口: HTTP 200 — {"code":0,"data":{}}
```

对照判断：
- **页面 ❌** → 当前标签页不是 DeepSeek 页面，先打开它
- **登录凭证 ❌** → 没找到登录态，先登录 `platform.deepseek.com`
- **导出接口 HTTP 401/403** → 凭证无效/过期，刷新页面重新登录
- **导出接口 HTTP 200 但 0 字节 / 非 ZIP** → 该月无数据或接口被改版
- 其他情况 → 把诊断结果发给我，我帮你分析❌（完全是用claude写的，你发给我我也不到啊，本作者纯小白一行代码看不懂，建议打包本项目+报错发给claude code/open code/codex等agent）

### 🔧 遇到「Could not establish connection. Receiving end does not exist」怎么办？

这个报错几乎都是因为 **DeepSeek 页面是在插件加载之前打开的**，content script 没有注入进去。
插件已经内置自动修复：点「同步」时若发现页面没有响应，会自动把 content script 注入该标签页再重试。

如果你仍然遇到，按顺序排查：
1. **重新加载插件**：改过代码后必须到 `chrome://extensions` 点该插件卡片上的 **⟳ 刷新** 按钮
2. **刷新 DeepSeek 页面**：按 `F5` 重新加载 `platform.deepseek.com`，让 content script 重新注入
3. 确认当前激活的标签页就是 `platform.deepseek.com`（不是其他网页）
4. 确认已经**登录** DeepSeek 后台

## 🛠 工作原理（简要）

插件在 `platform.deepseek.com` 页面内运行，复用你已登录的会话凭证（`userToken`），
直接调用后台私有接口：

```
GET /api/v0/usage/export?start=<utc>&end=<utc>&tz=0
    → 返回 ZIP，内含 amount-*.csv（每行：utc_date, model, api_key_name, type, price, amount）
GET /api/v0/usage/amount?month=&year=   （CSV 导出不可用时的 JSON 兜底）
```

如果标签页里没有 content script（插件安装后才打开的页面不存在此问题），插件会用
`chrome.scripting` 自动注入，所以**不需要手动刷新页面**。

这些接口是后台页面自身使用的，**非官方公开 API**，DeepSeek 改版时可能变化——
届时插件可能显示「认证失败」或「无数据」，刷新页面重新登录即可；若持续失败，请到项目 Issue 反馈。

## 🧪 本地测试与预览

```bash
# 1. 全部单测（CSV 解析 / content 同步 / 注入回退）
node scripts/test-csv.js
node scripts/test-content.js
node scripts/test-injection.js

# 2. 浏览器预览 Dashboard（模拟数据）
npx http-server . -p 3457
# 然后打开 http://localhost:3457/demo/demo.html
```

## ⚠️ 隐私与注意

- 所有数据仅存本地 IndexedDB，可通过插件弹窗 **🗑 清除** 一键清空
- 插件只请求 `platform.deepseek.com` 域（见 `manifest.json` 的 `host_permissions`）
- 请不要把你的 `userToken` / API Key 发给任何人
